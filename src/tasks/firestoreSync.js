import { getFirestoreDB } from '../services/firebase.js';

export async function initializeFirestoreSync() {
    // Por ahora solo inicializamos, la funcionalidad se añadirá más tarde
    console.log('🔄 Sincronización con Firestore inicializada');
} 